//item model
export class ItemModel {
    constructor(itemId, itemName, price, qty) {
        this.itemId = itemId;
        this.itemName = itemName;
        this.price = price;
        this.qty = qty;
    }
}